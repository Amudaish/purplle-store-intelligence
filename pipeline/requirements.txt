# Detection & Tracking
ultralytics==8.2.0          # YOLOv8 — person detection
supervision==0.21.0         # ByteTrack wrapper + annotation utilities

# Re-Identification
onnxruntime==1.18.0         # OSNet inference (ONNX model)
scipy==1.13.0               # Cosine similarity

# Computer Vision
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# Redis (event publishing + embedding store)
redis==5.1.0

# HTTP (publish events to API)
httpx==0.27.0

# Config & Utilities
python-dotenv==1.0.1
structlog==24.4.0
tqdm==4.66.4
